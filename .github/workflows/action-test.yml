name: Action Test

on: pull_request

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Hello
        id: hello
        uses: morikiyo/hello-world-docker-action
        with:
          who-to-greet: 'RUBY'
